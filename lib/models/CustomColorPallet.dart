import 'dart:ui';

class CustomColorPallete {
  Color  color;

  bool  isSelected;
  String hexColor;


  CustomColorPallete({required this.color,  required this.hexColor,required this.isSelected,});
}